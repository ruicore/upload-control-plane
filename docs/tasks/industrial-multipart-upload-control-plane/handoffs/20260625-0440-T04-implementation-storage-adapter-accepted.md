# Handoff: T04 storage adapter implementation

Status: accepted
Agent type: Implementation
Branch: main
Worktree: D:\upload-control-plane
Started: 2026-06-25 04:30 +08:00
Finished: 2026-06-25 04:40 +08:00

## Scope

- Intended scope:
  - Add boto3/botocore infrastructure adapter for the accepted domain `ObjectStorage` protocol.
  - Build separate internal and presign S3 clients from `S3_ENDPOINT_URL` and `S3_PUBLIC_ENDPOINT_URL`.
  - Implement create multipart upload, presign upload part, paged list parts, complete multipart upload, abort multipart upload, and head object.
  - Map provider responses and botocore errors into domain storage DTOs/exceptions.
  - Add unit tests for factory/config behavior, response mapping, and error mapping.
  - Add skippable MinIO integration test that runs the real multipart flow through a host-reachable presigned URL.
- Explicitly out of scope:
  - Public upload APIs.
  - CLI uploader.
  - Upload task/session DB application service.
  - File-byte endpoint through FastAPI.
  - MQTT/Go/edge.
- PRD/task files read:
  - `docs/tasks/industrial-multipart-upload-control-plane/agent-orchestration.md`
  - `docs/tasks/industrial-multipart-upload-control-plane/README.md` T04 section
  - `docs/tasks/industrial-multipart-upload-control-plane/handoffs/20260625-0430-T04-merge-storage-interface-accepted.md`
  - `docs/prd/industrial-multipart-upload-control-plane/01-non-negotiable-decisions.md`
  - `docs/prd/industrial-multipart-upload-control-plane/03-system-architecture.md`
  - `docs/prd/industrial-multipart-upload-control-plane/06-api-contracts.md`
  - `docs/prd/industrial-multipart-upload-control-plane/08-storage-adapter-and-object-keys.md`
  - `docs/prd/industrial-multipart-upload-control-plane/09-security-governance.md`
  - `docs/prd/industrial-multipart-upload-control-plane/10-retry-resume-completion-lifecycle.md`
  - `docs/prd/industrial-multipart-upload-control-plane/11-client-and-backend-implementation.md`
  - `src/upload_control_plane/domain/storage.py`
  - `src/upload_control_plane/config.py`
  - `docker-compose.yml`
  - `pyproject.toml`

## Changes

- Files changed:
  - `pyproject.toml`
  - `uv.lock`
  - `src/upload_control_plane/infrastructure/storage/__init__.py`
  - `src/upload_control_plane/infrastructure/storage/s3_minio.py`
  - `tests/infrastructure/test_s3_storage.py`
  - `tests/integration/test_s3_storage_minio.py`
  - `docs/tasks/industrial-multipart-upload-control-plane/handoffs/20260625-0440-T04-implementation-storage-adapter-accepted.md`
- Behavior changed:
  - Added runtime dependency `boto3` and dev typing dependency `boto3-stubs[s3]`.
  - Added `S3ObjectStorage`, satisfying the provider-neutral `ObjectStorage` protocol.
  - Added `build_s3_clients(settings)` with separate internal and presign clients.
  - Internal control operations use the internal client; presigned upload-part URLs are generated by the presign client configured with the public endpoint.
  - No signed URL host string replacement is used.
  - `ClientError` and `BotoCoreError` are mapped to domain storage exceptions with operation, provider code, and retryable metadata.
  - Integration test performs create -> presign -> direct PUT bytes to MinIO -> list -> complete -> head -> read verify.
- Compatibility notes:
  - Domain layer remains free of boto3/botocore imports.
  - No API routes, database schema, public upload APIs, or file-byte proxy behavior were added.
  - Integration test is marked `integration` and skips cleanly when local MinIO is unavailable.

## Verification

- Commands run:
  - `uv run pytest tests/infrastructure/test_s3_storage.py tests/integration/test_s3_storage_minio.py`
  - `uv run ruff check`
  - `uv run ruff format --check`
  - `uv run mypy src tests`
  - `uv run pytest`
  - `make test`
  - `docker compose config --quiet`
  - `docker compose up -d minio minio-init`
  - `uv run pytest tests/integration/test_s3_storage_minio.py -q`
  - `docker compose down`
- Results:
  - Focused storage pytest before MinIO: 7 passed, 1 skipped.
  - `uv run ruff check`: passed.
  - `uv run ruff format --check`: passed, 61 files already formatted.
  - `uv run mypy src tests`: passed, no issues found in 53 source files.
  - `docker compose config --quiet`: passed.
  - `docker compose up -d minio minio-init`: passed; MinIO became healthy and bucket init completed.
  - MinIO integration test against compose: passed, 1 passed.
  - `uv run pytest` with MinIO online: passed, 127 passed, 4 skipped, 1 existing FastAPI/Starlette TestClient deprecation warning.
  - `make test`: passed; repeated ruff, format check, mypy, and pytest with 127 passed, 4 skipped, 1 existing warning.
  - `docker compose down`: passed; minio, minio-init, and compose network removed.
- Commands not run and why:
  - Full API upload lifecycle tests were not run because T05/T06 public upload/session APIs are out of scope and not implemented.

## PRD Hard Constraints Check

- Backend/MQTT receives no file bytes:
  - Preserved. No API route, request body stream, UploadFile, proxy, or MQTT path was added. The only byte transfer in tests is a direct client PUT to MinIO through a presigned URL.
- Clients receive no MinIO/S3 credentials:
  - Preserved. The adapter returns presigned URLs only; credentials remain in backend settings/client construction.
- Complete uses object storage ListParts as authority:
  - Preserved at the adapter layer. The integration test builds completion parts from storage-observed `list_parts`.
- Authorization uses permission_grants:
  - Preserved. No authorization behavior was changed.
- Internal IDs remain UUIDs:
  - Preserved. No schema or identifier behavior was changed.
- MQTT/Go/edge remain optional and dependency-gated:
  - Preserved. No optional component was added.

## Risks and Follow-up

- Remaining risks:
  - Conditional complete, native checksum, object-lock, and advanced encryption policy paths are mapped narrowly and need policy-specific integration tests before enabling in production-like settings.
  - Adapter exposes one provider page for `list_parts`; T06 completion orchestration must keep paginating until storage is fully observed.
  - `CompletedObject.size_bytes` remains `None` after S3 complete because MinIO/S3 complete responses do not return object size; callers should use `head_object` when size is needed.
- Known gaps:
  - No public API integration yet because T05/T06 are not in scope.
  - No CORS inspection/configuration workflow yet.
  - No incomplete multipart sweeper implementation yet.
- Suggested next agent:
  - T04 storage validation agent can start full validation.

## Recovery Notes

- If accepted, next dependency unlocked:
  - Full T04 validation can start from this implementation.
- If partial, reusable pieces:
  - Not applicable.
- If blocked, unblock condition:
  - Not applicable.
- If rejected, do not repeat:
  - Do not generate presigned URLs from the internal `http://minio:9000` client and rewrite hosts afterward.
  - Do not add boto3 imports to the domain layer.
  - Do not add public upload APIs or FastAPI file-byte endpoints as part of storage adapter work.
